function Times(a, b, c) {

    let seconds = a * 3600 + b * 60 + c;
    console.log(seconds);
}

Times(5, 7, 0);


//реализовать как через время или как числа?, если как числа то как сделать if у переменой 01 то ноль опускаем также у трех переменных